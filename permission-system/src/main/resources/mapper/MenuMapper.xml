<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.xiaoxu.mapper.MenuMapper">


    <select id="getPermissionCodeByMenuIds" resultType="java.lang.String">
        SELECT permission_code
        FROM system_menu
        WHERE id IN
        <foreach item="menuIds" index="menuId" collection="list"
                 open="(" separator="," close=")">
            #{menuId}
        </foreach>
    </select>
    <select id="getPermissionCodeByUserId" resultType="java.lang.String">
        SELECT DISTINCT sm.permission
        FROM system_menu sm
                 JOIN system_role_menu srm ON sm.id = srm.menu_id
                 JOIN system_user_role ur ON srm.role_id = ur.role_id
        WHERE ur.user_id = #{userId}

    </select>
</mapper>