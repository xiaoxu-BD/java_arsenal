server:
  port: 16888
spring:
  application:
    name: server-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: server-user
          uri: lb://server-user
          predicates:
            - Path=/api/user/**
        - id: server-order
          uri: lb://server-order
          predicates:
            - Path=/order/**
          filters:
          - PrefixPath=/order
      globalcors:
        cors-configurations:
          '[/**]': # '[/**]' 匹配所有路径
            allowed-origins: "http://localhost:5173" # 允许的前端源地址，不能用*如果allowCredentials为true
            allowed-methods: # 允许的HTTP方法
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowed-headers: "*" # 允许所有的请求头
            allow-credentials: true # 是否允许携带Cookie
            max-age: 3600 # 预检请求的缓存时间（秒）
  data:
    redis:
      host: 124.221.151.196
      port: 6379
logging:
  level:
    root: info