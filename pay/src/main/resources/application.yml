spring:
  datasource:
    username: root
    url: jdbc:mysql://192.168.200.128/2026mysql_ds?serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    password: NFTurbo666
    driver-class-name: com.mysql.cj.jdbc.Driver
# MyBatis配置
mybatis:
  configuration:
    map-underscore-to-camel-case: true # 将数据库字段的下划线命名转换为驼峰命名
  mapper-locations: classpath:mapper/*.xml # MyBatis映射文件所在的位置，这里是使用XML的配置方式时需要配置的部分